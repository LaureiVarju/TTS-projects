console.log("JS File successfully linked");

var mom = moment();
console.log(mom.format("YYYY MM DD"));

//this doesn't work because I'm not linking to the library that defines the moment function. I though JS already did that
//oops

/* 
//This is a lodash function

let outputnum = _.random(1, 100);
console.log(outputnum);

*/
